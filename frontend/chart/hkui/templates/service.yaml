apiVersion: v1
kind: Service
metadata:
  name: {{ include "hkui.fullname" . }}
  labels:
    {{- include "hkui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hkui.selectorLabels" . | nindent 4 }}
