apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hkui.fullname" . }}-test-connection"
  labels:
    {{- include "hkui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hkui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
